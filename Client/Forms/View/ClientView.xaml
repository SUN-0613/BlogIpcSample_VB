<Window x:Class="Forms.View.ClientView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Client.Forms.ViewModel"
        mc:Ignorable="d"
        Title="ClientView" Height="150" Width="300">
    <Window.DataContext>
        <vm:ClientViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="Result = " VerticalAlignment="Center" TextAlignment="Right" />
        <TextBlock Grid.Column="1" Text="{Binding Result}" VerticalAlignment="Center" TextAlignment="Left" />

        <Button Grid.Row="1" Grid.ColumnSpan="2" Content="プロセス間通信実行" >
            <Button.InputBindings>
                <MouseBinding MouseAction="LeftClick" Command="{Binding ExecuteCommand}" />
            </Button.InputBindings>
        </Button>

    </Grid>
</Window>
